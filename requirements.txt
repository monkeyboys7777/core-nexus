# Core Nexus — Python Dependencies
# Install with: pip install -r requirements.txt
# Then run:     pip install mss pillow selenium webdriver-manager
#               pip install piper-tts sounddevice
#               pip install pvporcupine pvrecorder
#               pip install pygetwindow pywin32

# ── Core ───────────────────────────────────────────────────────────────────────
SpeechRecognition>=3.10.0     # Voice recognition (Google SR)
pyttsx3>=2.90                 # Text-to-speech fallback
pyaudio>=0.2.14               # Microphone input
psutil>=6.0.0                 # Process management + CPU stats
pycaw>=20240210               # Windows audio volume control
comtypes>=1.4.0               # COM interface (required by pycaw)

# ── Web UI ─────────────────────────────────────────────────────────────────────
flask>=3.0.0                  # Web UI server
flask-socketio>=5.3.0         # Real-time web events

# ── Vision ─────────────────────────────────────────────────────────────────────
mss>=10.0.0                   # Screenshot capture
pillow>=10.0.0                # Image processing

# ── Browser automation ─────────────────────────────────────────────────────────
selenium>=4.0.0               # Food ordering browser control
webdriver-manager>=4.0.0      # Auto-manages ChromeDriver

# ── Neural TTS ─────────────────────────────────────────────────────────────────
piper-tts>=1.2.0              # High-quality local neural voice
sounddevice>=0.4.6            # Audio playback for Piper
numpy>=1.24.0                 # Audio array processing

# ── Wake word ──────────────────────────────────────────────────────────────────
pvporcupine>=3.0.0            # Offline wake word detection
pvrecorder>=1.2.0             # Low-level audio recorder for Porcupine
# Free access key: https://console.picovoice.ai


# ── Window management ──────────────────────────────────────────────────────────
pygetwindow>=0.0.9            # Window position and size control
pywin32>=306                  # Windows API access
